#pragma once

#include <raylib.h>

namespace gyp {
const int DEFAULT_WIDTH = 1920;
const int DEFAULT_HEIGH = 1080;
const int DEFAULT_TRACK_NUM = 4;
const Color DEFAULT_FG = BLACK;
const Color DEFAULT_BG = WHITE;
const int DEFAULT_GEOMETRY = -1;
const int DEFAULT_THICKNESS = 5;
const int DEFAULT_SPEED = 1;
const int DEFAULT_FONT_SIZE = 20;
} // namespace gyp
